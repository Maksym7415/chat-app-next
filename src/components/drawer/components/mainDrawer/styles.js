import styled from "@mui/system/styled";
import Box from "@mui/material/Box";
import ListItemText from "@mui/material/ListItemText";

export const SDWLangs = styled(Box)(() => ({
	padding: "15px",
}));

export const SDListItem = styled(ListItemText)(() => ({
	width: "100%",
	cursor: "pointer",
}));
